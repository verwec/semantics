require 'spec_helper'

describe Semantics do
  it 'has a version number' do
    expect(Semantics::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
